import NumParts from "./NumParts";

export default interface NumWithComplement {
  num: string | NumParts;
  isComplement: boolean;
}

export default interface NumPartWithComplement {
  num: string | NumParts;
  isComplement: boolean;
}
